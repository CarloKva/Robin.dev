# Robin.dev — Task Board

> Task identificate dall'health check di `apps/orchestrator` (2026-03-05).
> Prefix: `TASK-REFACTOR-ORC-XX`. Prerequisiti Sprint C marcati HIGH.

---

### TASK-REFACTOR-ORC-01 · Rimuovi `waitForOrchestratorHealth()` ghost da hetzner.service.ts

**Priorità:** HIGH | **Effort:** XS
**Area:** `apps/orchestrator/src/services/hetzner.service.ts`

**Problema:**
`waitForOrchestratorHealth()` (riga 159) è esportata e menzionata nel commento di `index.ts:118`
("polled by waitForOrchestratorHealth()"), ma non è mai importata né chiamata. Il provisioning
worker ha sostituito questo approccio con `waitForAgentHeartbeat()` (heartbeat via Supabase DB),
ma la funzione originale è rimasta creando confusione su quale sia il meccanismo attivo.

**Criteri di accettazione:**
- [ ] `waitForOrchestratorHealth()` rimossa da `hetzner.service.ts`
- [ ] Commento in `index.ts:118` aggiornato per riflettere il meccanismo heartbeat attivo
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Verificare prima che `waitForOrchestratorHealth` non sia importata da nessun altro file
del monorepo (grep su tutto il repo). Se `apps/web` la importa da shared-types, fare
attenzione — ma da analisi del codice non risulta.

---

### TASK-REFACTOR-ORC-02 · Centralizza JWT GitHub in claude.runner.ts

**Priorità:** HIGH | **Effort:** S
**Area:** `apps/orchestrator/src/agent/claude.runner.ts`

**Problema:**
`ClaudeRunner.generateInstallationToken()` (riga 288) reimplementa da zero la generazione
del JWT GitHub App (crypto + fetch) che è già centralizzata e testata in
`src/services/github.service.ts:getInstallationToken()`. Le due implementazioni sono identiche.
Se l'endpoint o il formato JWT cambia, va aggiornato in entrambi i posti.

**Criteri di accettazione:**
- [ ] `ClaudeRunner.generateInstallationToken()` rimossa
- [ ] `ClaudeRunner.buildAuthenticatedCloneUrl()` chiama `getInstallationToken()` da
  `../services/github.service`
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Dipende da nessun'altra task. Eseguibile in isolamento.

---

### TASK-REFACTOR-ORC-03 · Fix: github-events.worker usa taskRepository per aggiornare stati

**Priorità:** HIGH | **Effort:** S
**Area:** `apps/orchestrator/src/workers/github-events.worker.ts`

**Problema:**
Le righe 166–176 e 195–204 aggiornano `tasks.status` direttamente via `db.from("tasks").update()`
bypassando `taskRepository.updateStatus()` e la state machine `VALID_TRANSITIONS`. Questo è
un bug: transizioni invalide (es. `cancelled → done`) vengono applicate senza controllo.
Inoltre duplica la logica di aggiornamento stato già centralizzata nel repository.

**Criteri di accettazione:**
- [ ] Le due update inline (`status: "done"`, `status: "in_review"`) sostituite con
  `taskRepository.updateStatus(taskId, "done")` e `taskRepository.updateStatus(taskId, "in_review")`
- [ ] Verificare che `"done"` sia in `VALID_TRANSITIONS["in_review"]` (già presente — ok)
- [ ] Verificare che `"in_review"` sia in `VALID_TRANSITIONS["review_pending"]` (attuale: no —
  aggiungere se necessario, oppure gestire il caso con try/catch e log)
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Questo è il bug più critico prima di Sprint C (rework flow). Il handler per
`pull_request_review_comment` e `issue_comment` è previsto in Sprint C — la state machine
deve essere corretta prima di implementarlo. Eseguire prima di iniziare Sprint C.

---

### TASK-REFACTOR-ORC-04 · Sposta `GitHubEventJobPayload` in shared-types

**Priorità:** MEDIUM | **Effort:** XS
**Area:** `apps/orchestrator/src/workers/github-events.worker.ts` + `packages/shared-types/src/index.ts`

**Problema:**
`GitHubEventJobPayload` (riga 25–29) è definita localmente nel worker invece che in
`packages/shared-types`. Il produttore di questo job (`apps/web/app/api/webhooks/github`)
non ha un tipo condiviso per costruire il payload, aumentando il rischio di disallineamento.

**Criteri di accettazione:**
- [ ] `GitHubEventJobPayload` aggiunta a `packages/shared-types/src/index.ts`
- [ ] `github-events.worker.ts` importa il tipo da `@robin/shared-types`
- [ ] `apps/web` webhook route aggiornata per usare il tipo condiviso (se applicabile)
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Prerequisito per Sprint D se il routing webhook diventa workspace-aware.

---

### TASK-REFACTOR-ORC-05 · Rimuovi metodi morti dai repository (heartbeat, appendEvent, getByWorkspace)

**Priorità:** LOW | **Effort:** XS
**Area:** `apps/orchestrator/src/repositories/`

**Problema:**
Tre metodi definiti ma mai chiamati:
- `AgentRepository.heartbeat()` (`agent.repository.ts:34`) — HeartbeatService bypassa questo
- `AgentRepository.getByWorkspace()` (`agent.repository.ts:45`) — non chiamata
- `TaskRepository.appendEvent()` (`task.repository.ts:111`) — sostituita da EventService

Aumentano il debito cognitivo e possono confondere su quale sia il percorso corretto per
scrivere eventi o aggiornare heartbeat.

**Criteri di accettazione:**
- [ ] I tre metodi rimossi (o spostati in una sezione `// Reserved for future use` documentata
  se si vuole mantenerli intenzionalmente)
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Verificare con grep che non siano chiamati da `apps/web`. Se lo sono, non rimuoverli.

---

### TASK-REFACTOR-ORC-06 · Unifica getTimeoutForType in bullmq.config.ts

**Priorità:** LOW | **Effort:** XS
**Area:** `apps/orchestrator/src/config/bullmq.config.ts` + `src/workers/repo-queue.worker.ts`

**Problema:**
`getTimeoutForType()` in `repo-queue.worker.ts:231` e `defaultTimeoutByType` in
`bullmq.config.ts:41` mappano entrambi task type → minuti di timeout con logica
quasi identica. La versione in repo-queue ha 2 entry in più (`accessibility: 30`, `security: 45`).

**Criteri di accettazione:**
- [ ] `defaultTimeoutByType` in `bullmq.config.ts` esteso con le entry mancanti
- [ ] `getTimeoutForType()` in `repo-queue.worker.ts` rimossa, sostituita da import di
  `defaultTimeoutByType`
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

---

### TASK-REFACTOR-ORC-07 · Rimuovi dipendenze npm inutilizzate (resend, zod)

**Priorità:** LOW | **Effort:** XS
**Area:** `apps/orchestrator/package.json`

**Problema:**
- `resend` (`^4.1.2`): `NotificationService` usa `fetch` diretto sull'API Resend, non il
  package npm. Il package è installato ma mai importato.
- `zod` (`^3.24.1`): nessun file in `src/` importa zod. Probabilmente residuo di
  validazione job payload pianificata ma non implementata.

**Criteri di accettazione:**
- [ ] `resend` rimosso da `dependencies` in `package.json`
- [ ] `zod` rimosso da `dependencies` in `package.json`
- [ ] `npm install` eseguito per aggiornare `package-lock.json`
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Se in Sprint C si decide di usare il package `resend` per il rework notification, questa
task è bloccante — aspettare decisione architetturale prima di procedere.

---

### TASK-REFACTOR-ORC-08 · Centralizza worker options in bullmq.config.ts

**Priorità:** LOW | **Effort:** S
**Area:** `apps/orchestrator/src/config/bullmq.config.ts` + tutti i file worker

**Problema:**
Solo `task.worker.ts` usa `workerOptions` da `bullmq.config.ts`. I 5 worker rimanenti
hardcodano le opzioni BullMQ inline (concurrency, stalledInterval, maxStalledCount, autorun).
Modificare una policy BullMQ richiede aggiornare più file.

**Criteri di accettazione:**
- [ ] `bullmq.config.ts` esteso con opzioni per provisioning worker, deprovisioning worker,
  sprint-control worker, github-events worker, repo-queue worker
- [ ] Ogni worker factory usa le opzioni centralizzate
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Le opzioni per ogni worker sono diverse (concurrency varia da 1 a 5); la centralizzazione
deve preservare questi valori distinti, non uniformarli. Non raggruppare in un unico oggetto
se le differenze sono intenzionali.

---

### TASK-REFACTOR-ORC-09 · Fix static import di task.worker.ts sulla control-plane

**Priorità:** MEDIUM | **Effort:** S
**Area:** `apps/orchestrator/src/index.ts`

**Problema:**
`task.worker.ts` e `task.queue.ts` sono importati staticamente in `index.ts` (righe 6–7)
anche in modalità control-plane. Questo causa:
1. Validazione AGENT_ID a module-load time anche sul CP (potenziale crash se AGENT_ID invalido)
2. `new TaskQueue()` apre una connessione BullMQ Queue sul CP senza utilizzo reale
3. Accoppiamento inutile per Sprint D

**Criteri di accettazione:**
- [ ] Import di `createWorker`, `createQueueEventMonitor` da `task.worker.ts` spostato
  nel branch `else` (agent mode) di `index.ts` — come dynamic import o refactor delle
  funzioni di bootstrap
- [ ] Import di `taskQueue` reso condizionale: usato solo in agent mode (e per Bull Board
  in agent mode)
- [ ] Validazione AGENT_ID rimossa da module-level in `task.worker.ts` — lasciata solo
  nel branch agent di `index.ts`
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale in entrambe le modalità
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
Questo task ha complessità moderata perché Node.js non supporta import condizionali top-level
in CommonJS in modo diretto. Valutare dynamic `require()` condizionale o estrazione in
funzione `bootstrapAgent()` / `bootstrapControlPlane()`.

---

### TASK-REFACTOR-ORC-10 · Deduplica get-github-token.mjs nei cloud-init heredoc

**Priorità:** LOW | **Effort:** M
**Area:** `apps/orchestrator/src/services/hetzner.service.ts` + `src/services/snapshot-baker.service.ts`

**Problema:**
Il file `get-github-token.mjs` (script Node.js per generare token GitHub App) è embeddato
identicamente come stringa literal in due heredoc:
- `hetzner.service.ts:buildCloudInitScript()` (riga 323–347)
- `snapshot-baker.service.ts:bakeSnapshot()` (riga 101–125)

Modificare la logica di generazione token richiede aggiornare entrambe le stringhe.

**Criteri di accettazione:**
- [ ] Il contenuto di `get-github-token.mjs` estratto in una costante condivisa o in una
  funzione `buildGitHubTokenScript()` in `hetzner.service.ts`
- [ ] `snapshot-baker.service.ts` importa e riusa la costante/funzione invece di duplicarla
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale
- [ ] Worker si avvia e processa job correttamente dopo la modifica

**Note:**
L'effort è M perché la stringa è multilinea con template literals annidati — va gestita
con cura per non rompere l'escaping bash. Testare il cloud-init risultante su una VPS
di staging prima di mergiare.

---

## Refactor — apps/web (da Health Check 2026-03-05)

---

### TASK-REFACTOR-WEB-01 · Estrai helper `requireWorkspace()` per route handler

**Priorità:** HIGH
**Effort:** S
**Area:** `apps/web/lib/`, `apps/web/app/api/`

**Problema:**
Il blocco `auth() + getWorkspaceForUser() + error responses` è copiato
letteralmente in 31+ route handler. Non esiste un helper centralizzato.
Rischio di divergenza su messaggi di errore, HTTP status code e ordine delle
operazioni nei futuri handler di Sprint C e D.

Esempio del pattern ripetuto (ogni route handler):
```typescript
const { userId } = await auth();
if (!userId) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
const workspace = await getWorkspaceForUser(userId);
if (!workspace) return NextResponse.json({ error: "Workspace not found" }, { status: 404 });
```

**Criteri di accettazione:**
- [ ] Esiste `apps/web/lib/api/requireWorkspace.ts` con funzione `requireWorkspace()`
      che restituisce `{ userId, workspace }` oppure una `NextResponse` di errore
- [ ] Tutti i route handler in `app/api/` usano il nuovo helper
- [ ] TypeScript compila senza errori
- [ ] `npm run lint` e `npm run typecheck` passano
- [ ] Nessuna regressione funzionale

**Note:**
Completare prima di Sprint C e D. Con multi-tenancy (Sprint D) un unico punto di
verifica workspace riduce il rischio di bypass accidentale su nuovi handler.
Può essere fatto in parallelo con TASK-REFACTOR-WEB-02.

---

### TASK-REFACTOR-WEB-02 · Fix security — role check su route `/api/admin/*`

**Priorità:** HIGH
**Effort:** XS
**Area:** `apps/web/app/api/admin/`

**Problema:**
`app/api/admin/bake-snapshot/route.ts` e `app/api/admin/update-agents/route.ts`
verificano solo che l'utente sia autenticato e abbia un workspace, **ma non
controllano il ruolo** (`workspace_members.role`). Qualsiasi membro del workspace
può triggerare un VPS snapshot (operazione da 7-10 min) o forzare il restart di
tutti gli orchestratori attivi. Il campo `role = "owner"` esiste in `workspace_members`.

**Criteri di accettazione:**
- [ ] Entrambe le route verificano `workspace_members.role = 'owner'` prima di procedere
- [ ] Ritornano HTTP 403 con `{ error: "Forbidden" }` per non-owner
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale per utenti owner

**Note:**
Bug di sicurezza — alta priorità assoluta prima di Sprint D (multi-tenancy).
Non dipende da altre task.

---

### TASK-REFACTOR-WEB-03 · Elimina dead tree backlog (10 componenti + 1 hook)

**Priorità:** HIGH
**Effort:** S
**Area:** `apps/web/components/backlog/`, `apps/web/components/sprints/`, `apps/web/lib/realtime/`

**Problema:**
Una dead tree di componenti non è mai raggiunta dal router. Nata dall'evoluzione da
`BacklogSprintTabs` → `BacklogJiraView` (Sprint B), non è stata rimossa.

File da eliminare (verificato: 0 import attivi fuori dalla dead tree):
- `components/backlog/BacklogPageClient.tsx`
- `components/backlog/BacklogSprintTabs.tsx`
- `components/backlog/BacklogClient.tsx`
- `components/backlog/BacklogFilters.tsx`
- `components/backlog/SprintTabView.tsx`
- `components/backlog/SprintSection.tsx`
- `components/sprints/ActiveSprintBoard.tsx`
- `components/sprints/CreateSprintButton.tsx`
- `components/sprints/SprintArchiveTable.tsx`
- `components/sprints/TaskStatusCard.tsx`
- `lib/realtime/useTaskEvents.ts`

**Attenzione — NON eliminare:**
- `SprintPlanningView` — usato da `sprints/[sprintId]/page.tsx`
- `SprintProgressBar`, `SprintNameInlineEditor`, `SprintActiveTable` — usati dalla sprint detail page
- `BrainstormModal`, `CreateTaskDrawer`, `BulkActionBar`, `TaskRow` — usati da `BacklogJiraView`

**Criteri di accettazione:**
- [ ] I file listati eliminati
- [ ] `grep -r "BacklogPageClient\|BacklogSprintTabs\|BacklogClient\|BacklogFilters\|SprintTabView\|SprintSection\|ActiveSprintBoard\|CreateSprintButton\|SprintArchiveTable\|TaskStatusCard\|useTaskEvents" apps/web/app apps/web/components apps/web/lib` restituisce zero risultati
- [ ] `npm run build` completa senza errori
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione visiva in `/backlog` e `/sprints/[id]`

**Note:**
Nessuna dipendenza. Prima task consigliata — riduce il surface del codebase per Sprint C.

---

### TASK-REFACTOR-WEB-04 · Crea `lib/db/tasks.ts` e rimuovi query inline

**Priorità:** HIGH
**Effort:** S
**Area:** `apps/web/lib/db/`, `apps/web/app/(dashboard)/tasks/page.tsx`, `apps/web/app/(dashboard)/layout.tsx`

**Problema:**
Query Supabase raw su tabelle di dominio in file di presentazione invece del layer `lib/db/`:

1. `app/(dashboard)/tasks/page.tsx:63–96` — query complessa su `tasks` con filtri multipli
   + helper `periodStart()` inline. Non esiste `lib/db/tasks.ts`.
2. `app/api/tasks/route.ts:40–51` — query analoga (stessa struttura, filtri diversi).
   Le due query duplicano logica che dovrebbe stare in un unico DAL.
3. `app/(dashboard)/layout.tsx:39–55` — query su `agent_status` + `tasks` per lo status
   badge header. Dovrebbe stare in `lib/db/agents.ts`.

**Criteri di accettazione:**
- [ ] Esiste `apps/web/lib/db/tasks.ts` con `getTasksForWorkspace(workspaceId, filters)` centralizzata
- [ ] `tasks/page.tsx` usa la nuova funzione invece della query inline
- [ ] `app/api/tasks/route.ts` riusa la stessa funzione (o una variante) invece di duplicare
- [ ] Query agent status in `layout.tsx` spostata in funzione `getActiveAgentStatus(workspaceId)` in `lib/db/agents.ts`
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione funzionale

**Note:**
Necessario prima di Sprint C che aggiunge logica rework sui task. Con un layer
centralizzato aggiungere filtri su `status = 'rework'` è banale e in un solo posto.

---

### TASK-REFACTOR-WEB-05 · Estrai costanti `STATUS_LABELS`/`STATUS_COLORS`/`PRIORITY_ICONS`

**Priorità:** MEDIUM
**Effort:** XS
**Area:** `apps/web/lib/constants/`, `apps/web/components/backlog/`

**Problema:**
`STATUS_LABELS` (14 entries) e `STATUS_COLORS` (14 entries) sono definiti indipendentemente
in `BacklogJiraView.tsx` e `TaskRow.tsx` (entrambi file attivi). Idem `PRIORITY_ICONS`.
Un cambio al colore di uno status richiede modifica in più posti.

**Criteri di accettazione:**
- [ ] Esiste `apps/web/lib/constants/task-display.ts` con `STATUS_LABELS`, `STATUS_COLORS`,
      `PRIORITY_ICONS` esportati
- [ ] `BacklogJiraView.tsx` e `TaskRow.tsx` importano dal file condiviso
- [ ] TypeScript compila senza errori
- [ ] Nessuna regressione visiva

**Note:**
Utile prima di Sprint C che probabilmente aggiunge nuovi status (es. `rework` attivo
nell'UI). Bassa complessità, alto impatto sulla coerenza visiva.

---

### TASK-REFACTOR-WEB-06 · Correggi dynamic import Clerk in `task-templates/route.ts`

**Priorità:** MEDIUM
**Effort:** XS
**Area:** `apps/web/app/api/task-templates/route.ts`

**Problema:**
`task-templates/route.ts:12,23` usa dynamic import per Clerk auth:
```typescript
const { userId } = await (await import("@clerk/nextjs/server")).auth();
```
Unico file nel progetto con questo pattern. Tutti gli altri 31+ route handler
usano lo static import standard `import { auth } from "@clerk/nextjs/server"`.

**Criteri di accettazione:**
- [ ] Sostituito con static import `import { auth } from "@clerk/nextjs/server"` in testa al file
- [ ] TypeScript compila senza errori
- [ ] L'endpoint risponde correttamente

**Note:**
Task banale. Idealmente inclusa nella PR di TASK-REFACTOR-WEB-01.

---

### TASK-REFACTOR-WEB-07 · Chiarire stato `/dashboard` e `/sprints/[id]`

**Priorità:** LOW
**Effort:** XS
**Area:** `apps/web/app/(dashboard)/dashboard/`, `apps/web/app/(dashboard)/sprints/`

**Problema:**
- `app/(dashboard)/dashboard/page.tsx` — nessun link da sidebar né mobile nav.
  Non è chiaro se è da reintrodurre nel nav o da rimuovere.
- `app/(dashboard)/sprints/[sprintId]/page.tsx` — irraggiungibile dall'UI dopo
  TASK-REFACTOR-WEB-03. Componenti `SprintPlanningView`, `SprintActiveTable`,
  `SprintNameInlineEditor` dipendono da questa pagina.

**Criteri di accettazione:**
- [ ] Decisione documentata su `/dashboard`: aggiungere link nel sidebar OPPURE eliminare la page
- [ ] Decisione documentata su `/sprints/[id]`: aggiungere link (es. da BacklogJiraView sprint header)
      OPPURE eliminare la page + i 3 componenti dipendenti
- [ ] TypeScript compila senza errori

**Note:**
Richiede decisione product. Completare TASK-REFACTOR-WEB-03 prima. Se la sprint
detail page viene eliminata, rimuovere anche `SprintPlanningView`, `SprintActiveTable`,
`SprintNameInlineEditor` per ulteriore pulizia.

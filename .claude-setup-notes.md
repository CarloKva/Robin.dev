# Claude Code Setup — Note

**Data:** 2026-03-05

## Decisioni prese in autonomia

- **BUG-ORC-01 e BUG-ORC-02:** Il task originale documentava solo BUG-ORC-01. Ho aggiunto BUG-ORC-02 (rework trigger non implementati) perché è un'assenza funzionale esplicita nel codice (`github-events.worker.ts` riceve `pull_request_review_comment` e `issue_comment` ma li logga senza processarli). Ho documentato entrambi in tutti e tre i CLAUDE.md.

- **SECURITY-WEB-01 non incluso:** Il task chiedeva di documentare SECURITY-WEB-01 come known issue (admin routes senza role check). Dal codice il refactor 2026-03-05 ha già aggiunto `getWorkspaceMemberRole()` alle admin routes. L'ho quindi omesso dai known issues attivi. Verificare se effettivamente fixato prima di Sprint C.

- **`docs/flows/agent-provisioning.md` non trovato:** Il file non esiste — `docs/` contiene solo `architecture.md`, `events.md`, `onboarding.md`, `runbook.md`, `schema.md`, `security.md`, `testing.md`, `ux.md`. Il contenuto sul provisioning è distribuito tra `docs/architecture.md` (sezione ADR-11) e `docs/runbook.md` (sezione 5). Ho usato entrambe le fonti.

- **Prisma:** `lib/prisma.ts` esiste ma non è usato nelle query attuali. Ho documentato questo come debt per non creare false aspettative su quale client usare per nuove query.

- **`gitlab-events.worker.ts` — bug framing:** Il task originale diceva che il worker "bypassa la state machine per le transizioni done/in_review". Dal codice vedo un try/catch intorno a `taskRepository.updateStatus()` con messaggio "invalid transition to done — skipping". Questo suggerisce che `taskRepository.updateStatus()` ha una validazione. Il "bypass" potrebbe riferirsi a transizioni permesse che non dovrebbero esserlo (es. da `rework` → `done` direttamente). Ho documentato il bug come da specifica originale senza over-interpretare.

## Sezioni non compilate

- **`apps/web/CLAUDE.md` — shadcn/ui versione:** Non trovata esplicitamente in `package.json` — shadcn/ui installa i componenti come file copiati in `components/ui/`, non come dipendenza npm. Ho documentato solo "componenti in `components/ui/`".

- **`apps/orchestrator/CLAUDE.md` — pino versione:** Non trovata nel package.json (estratto parziale). Ho documentato come "logger strutturato JSON" senza versione.

- **Sprint C roadmap:** Non trovato un file `.robin.md` o un backlog Sprint C attivo al path `docs/product-roadmap-and-sprints/`. Il root `/CLAUDE.md` punta a `.robin.md` come backlog attivo (se esiste).

## Suggerimenti per il founder

1. **`SECURITY-WEB-01` — verificare stato:** Controllare se le admin routes `api/admin/bake-snapshot` e `api/admin/update-agents` hanno effettivamente il role check attivo dopo il refactor 2026-03-05. Se sì, rimuovere dal backlog dei known issues.

2. **`GitHubEventJobPayload` in shared-types:** Questo è un quick win (10 min) prima di Sprint C. Spostarla da `github-events.worker.ts` a `packages/shared-types/src/index.ts` rimuove una fonte di drift tra web e orchestrator.

3. **Rework trigger (BUG-ORC-02):** Il codice in `github-events.worker.ts` riceve già `pull_request_review_comment` e `issue_comment` ma li logga e scarta. L'infrastruttura è pronta — manca solo l'handler. Questo è il gate bloccante per Sprint C (iterazioni automatiche).

4. **`BacklogJiraView.tsx` (728 righe):** Prima di aggiungere feature allo sprint planning, considerare di estrarre almeno `SprintSection` inline come componente separato. Il file è già al limite della leggibilità.

5. **`exactOptionalPropertyTypes: true`:** Questa regola causa errori sottili in TypeScript. Documentata in `apps/web/CLAUDE.md` — assicurarsi che tutti i developer (e agenti) la leggano prima di modificare tipi o props.
